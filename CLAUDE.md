# Claude AI 開発ガイド

このファイルは、「もっと住みやすい高槻へ」プロジェクトを Claude 系AIが理解し、開発・保守作業を安全に行うための入口です。

このファイル自体が、政策データ構造やサイト設計の詳細を定義するものではありません。

Claudeは作業内容に応じて、以下に示す正式情報源を確認してください。

---

# 1. プロジェクト概要

本プロジェクトは、Astro + TypeScript + Markdownによって構築する、

**「もっと住みやすい高槻へ」**

の政策・活動・出典情報を公開するWebサイトです。

主な目的は、

* 政策を分かりやすく公開する
* 政策の根拠となる資料を示す
* 実際の活動と政策を関連付ける
* 市民が意見を届けられるようにする
* 長期的な情報資産としてサイトを育てる
* AIを活用しながら安全に保守できる構造を維持する

ことです。

---

# 2. Claudeが最初に確認する情報源

Claudeは、作業を開始する前に、必要な情報源を確認してください。

情報源には、それぞれ役割があります。

---

## 2-1. `AGENTS.md`

**AI開発全体の入口です。**

AIがこのプロジェクトで作業するときに、最初に確認する案内文書です。

以下を確認できます。

* プロジェクトの概要
* 正式情報源の一覧
* 各情報源の役割
* AIが作業するときの基本的な確認順序
* データ構造をどこで確認するか

`AGENTS.md` は、特定のAI製品に依存しない共通の入口として扱います。

---

# 3. 正式情報源の優先順位

プロジェクト内の情報は、目的によって参照する正式情報源が異なります。

単純に「このファイルが常に最優先」と考えるのではなく、**変更対象に対応する正式情報源を確認する**ことを原則とします。

---

## 3-1. データ構造・型を変更する場合

次の順序で確認します。

1. `src/content.config.ts`
2. 政策データ構造仕様書
3. 関連する既存コンテンツ
4. 関連する実装

### `src/content.config.ts`

Content Collectionsの**実装上の正式なスキーマ**です。

以下を確認します。

* `policies`
* `activities`
* `sources`
* 各フィールドの型
* 必須・任意
* デフォルト値
* 日付形式
* `category`
* `tags`
* `relatedActivities`
* `references`
* Content Collection reference

コード上の実際の型については、このファイルを基準とします。

---

## 3-2. 政策・活動・出典の意味を確認する場合

**政策データ構造仕様書**を確認します。

この仕様書は、

* 政策データ
* 活動データ
* 出典データ
* 各フィールドの意味
* 各データ同士の関係
* どの情報をどこに記録するか

を定義する正式仕様です。

`src/content.config.ts` が「どのような型で保存できるか」を定義するのに対し、

政策データ構造仕様書は「そのデータを何のために、どのように使うか」を定義します。

---

## 3-3. サイトの設計思想を確認する場合

`src/docs/01_設計原則.md`

を確認します。

この文書は、サイト全体の長期的な設計思想と、原則として変えない方針を定義します。

---

## 3-4. AIの行動ルールを確認する場合

`src/docs/08_AI運用ルール.md`

を確認します。

AIによる、

* コード変更
* コンテンツ作成
* ファクトチェック
* 出典管理
* UI変更
* SEO変更
* エラー対応
* レビュー
* 長期保守

などの実務ルールは、この文書を基準とします。

---

## 3-5. 政策文章を作成・修正する場合

`src/docs/02_コンテンツ執筆ルール.md`

を確認します。

---

## 3-6. 出典・根拠・事実確認を行う場合

`src/docs/03_エビデンス運用ルール.md`

を確認します。

---

## 3-7. UI・デザインを変更する場合

`src/docs/04_UI・デザインルール.md`

を確認します。

---

## 3-8. アクセシビリティを変更・確認する場合

`src/docs/05_アクセシビリティチェック.md`

を確認します。

---

## 3-9. SEO・情報設計を変更する場合

`src/docs/06_SEO・情報設計.md`

を確認します。

---

## 3-10. 公開・ビルド・保守を行う場合

`src/docs/07_公開・保守手順.md`

を確認します。

---

## 3-11. 新しい政策ページを作成する場合

`政策ページ作成テンプレート.md`

を実務上の作成補助として使用します。

ただし、このテンプレートは**正式なデータ構造仕様ではありません**。

データ構造については、

1. `src/content.config.ts`
2. 政策データ構造仕様書

を優先します。

テンプレートと正式仕様が矛盾する場合、テンプレートを勝手に優先せず、矛盾を確認してください。

---

# 4. 情報源の役割を混同しない

Claudeは、以下を混同してはいけません。

| 情報源                     | 主な役割                       |
| ----------------------- | -------------------------- |
| `AGENTS.md`             | AI開発全体の入口・案内               |
| `CLAUDE.md`             | Claude系AI向けの開発入口           |
| `src/content.config.ts` | Content Collectionsの実装スキーマ |
| 政策データ構造仕様書              | 政策・活動・出典データの意味と構造          |
| `01_設計原則.md`            | サイト全体の不変的な設計思想             |
| `02_コンテンツ執筆ルール.md`      | 文章作成・編集のルール                |
| `03_エビデンス運用ルール.md`      | 出典・根拠・ファクトチェックのルール         |
| `04_UI・デザインルール.md`      | UI・デザインのルール                |
| `05_アクセシビリティチェック.md`    | アクセシビリティの確認基準              |
| `06_SEO・情報設計.md`        | SEO・情報設計のルール               |
| `07_公開・保守手順.md`         | ビルド・公開・保守の手順               |
| `08_AI運用ルール.md`         | AIが作業するときの実務ルール            |
| `政策ページ作成テンプレート.md`      | 政策ページ作成時の実務テンプレート          |

---

# 5. AIによる変更の基本原則

Claudeは、既存コードを推測だけで変更してはいけません。

原則として、次の順序で作業します。

1. 目的を確認する
2. 対象ファイルを確認する
3. 現在の実装を確認する
4. 関連する正式情報源を確認する
5. 変更による影響範囲を確認する
6. 変更範囲を限定する
7. 必要最小限の変更を行う
8. `npm run build` を実行する
9. 必要に応じてブラウザで確認する
10. 変更内容を記録する

確認していないファイルについて、現在の内容を推測して断定しません。

---

# 6. データ構造変更時の原則

政策・活動・出典のデータ構造を変更する場合は、特に慎重に扱います。

例えば、

```text
updated
category
tags
relatedActivities
references
problem
evaluation
issues
proposals
limitations
```

などのフィールドについて、Claudeが独自の名前や構造を追加してはいけません。

新しいフィールドが必要だと判断した場合は、

1. なぜ必要なのかを整理する
2. 政策データ構造仕様書との整合性を確認する
3. `src/content.config.ts` の変更が必要か確認する
4. 既存コンテンツへの影響を確認する
5. 設計変更が必要なら、先に設計を確定する
6. その後に実装する

という順序で進めます。

---

# 7. Content Collection referenceの扱い

`policies`、`activities`、`sources` の間には、Content Collection referenceによる関連付けがあります。

例えば、

```text
relatedActivities
references
```

などを変更すると、関連するCollectionにも影響する可能性があります。

そのため、referenceを変更する場合は、

* 参照元
* 参照先
* ID
* slug
* 対象ファイル
* 表示側の取得処理

を確認します。

存在しないreferenceを推測で追加してはいけません。

---

# 8. ファクトチェック

Claudeの回答や生成した文章そのものを、公開情報の出典として扱いません。

以下については、公開前に一次資料または信頼性の高い資料を確認します。

* 法律
* 条例
* 行政制度
* 統計
* 医療・健康情報
* 公的支援制度
* 行政の実施状況
* 費用
* 日付
* 組織名
* URL

事実・解釈・意見・政策提案を区別します。

確認できていない情報については、断定しません。

---

# 9. コード編集時のルール

コード変更を提案するときは、可能な限り次を明確にします。

* 変更の目的
* 対象ファイル
* 変更対象の範囲
* 変更前の目印
* 変更後のコード
* 触らない場所
* ブラウザで確認する項目
* `npm run build` の必要性
* コミットの要否

変更箇所が不明確な場合は、現在のファイル内容を確認してから指示します。

「適当な場所に追加する」など、利用者の判断に依存する指示は避けます。

---

# 10. 一度に変更する範囲

原則として、1回の作業では1つの目的だけを扱います。

例：

* データスキーマの変更
* PolicyHeroの改善
* 出典データの追加
* 政策本文の修正
* SEO設定の変更

などを可能な限り分離します。

複数ファイルの変更が必要な場合は、

* 変更順
* 依存関係
* 確認方法

を先に示します。

大規模な一括変更は避け、まず代表ページで確認してから展開します。

---

# 11. ビルド確認

コードまたはContent Collectionに関係する変更を行った場合は、原則として、

```text
npm run build
```

を実行します。

ビルドが成功した場合でも、必要に応じて対象ページをブラウザで確認します。

ビルドエラーが発生した場合は、エラーを確認して原因を切り分けます。

複数箇所を推測で同時に変更してはいけません。

---

# 12. エラー対応

エラーが発生した場合は、可能な限り次の情報を確認します。

* ファイル名
* 行番号
* エラー全文
* 実行したコマンド
* 直前に変更した内容
* 該当コード

原因を特定する前に、複数箇所を無関係に変更しません。

---

# 13. 設計変更

現在の実装と設計書が一致していない場合、単純にコードを設計書へ合わせるとは限りません。

まず、

1. 現在の実装
2. 現在の設計書
3. 実際の利用状況
4. 変更による影響

を確認します。

設計を変更する場合は、

**設計書 → 実装 → 確認**

の順序を基本とします。

設計書同士に矛盾がある場合は、実装前に矛盾を整理します。

---

# 14. 政策コンテンツ

政策コンテンツを作成・修正するときは、必要に応じて、

* 政策データ構造仕様書
* `src/content.config.ts`
* `src/docs/01_設計原則.md`
* `src/docs/02_コンテンツ執筆ルール.md`
* `src/docs/03_エビデンス運用ルール.md`
* `政策ページ作成テンプレート.md`

を確認します。

特に、

**事実・現場経験・評価・提案・出典**

を混同しないことを重視します。

---

# 15. 出典・参照関係

政策に出典を追加する場合は、既存のsources Collectionを確認します。

新しい出典を追加する場合は、可能な限り、

* 資料タイトル
* 発行主体
* 発行年または更新情報
* URL

を確認します。

政策と活動を関連付ける場合は、既存のContent Collection referenceを利用し、存在しないIDを作らないようにします。

---

# 16. UI・アクセシビリティ

UIを変更する場合は、

* `src/docs/04_UI・デザインルール.md`
* `src/docs/05_アクセシビリティチェック.md`

を確認します。

色やアイコンだけで情報を伝えないことを基本とします。

---

# 17. SEO・情報設計

ページを追加・変更する場合は、必要に応じて、

* title
* description
* h1
* URL
* 内部リンク
* 更新日
* 出典
* canonical
* OGP
* 構造化データ

の整合性を確認します。

---

# 18. 個人情報・市民の声

AIへ入力する情報には、必要のない個人情報を含めません。

市民の声を扱う場合は、

* 氏名
* 住所
* 電話番号
* メールアドレス
* 個人を特定できる情報
* 必要以上の病歴・家庭事情

などを不要に共有・公開しません。

本人の許可なく、個人が特定できる原文を公開しません。

---

# 19. セキュリティ

Claudeへ次の情報を入力しません。

* パスワード
* APIキー
* 秘密鍵
* アクセストークン
* 管理画面の認証情報
* 不要な個人情報

外部パッケージやコードを導入する場合は、必要性と安全性を確認します。

---

# 20. Claudeが判断できない場合

次の場合、Claudeは勝手に決定しません。

* 設計書同士が矛盾している
* 現在のコードを確認できない
* データ構造の意図が不明
* 公開情報の真偽を確認できない
* 既存仕様を変更する必要がある
* 複数の設計方針があり影響が大きい

この場合は、

1. 問題点
2. 確認できている事実
3. 不明点
4. 必要な確認事項
5. 推奨案

を整理して提示します。

---

# 21. 長期保守

このプロジェクトでは、AIそのものを固定することを目的としません。

将来、Claude以外のAIを使用する可能性があります。

そのため、

* 設計思想
* データ構造
* AI運用ルール
* 公開・保守手順
* 重要な設計判断

を会話履歴だけに依存させません。

重要な判断は、対応する正式な設計書へ記録します。

目標は、

**「AIが変わっても、プロジェクトの品質と設計思想を維持できる状態」**

です。

---

# 22. Claudeへの最終確認

作業完了前に、次を確認します。

### 内容

* 事実と意見を区別したか
* 必要な出典があるか
* 根拠のない断定をしていないか
* 政策の限界を必要に応じて示したか

### データ

* `src/content.config.ts` と整合しているか
* 政策データ構造仕様書と整合しているか
* referenceが正しいか
* 不要なフィールドを追加していないか

### 技術

* TypeScriptエラーがないか
* Content Collectionエラーがないか
* `npm run build` が成功したか
* 既存ページが壊れていないか

### 保守

* 変更範囲が必要最小限か
* 関連する設計書を確認したか
* 設計変更が必要なら設計書を先に扱ったか
* 将来のAIが理解できる状態になっているか

---

# 23. 基本方針

Claudeは、

**「動けばよい」ではなく、「数年後に人間とAIの双方が理解・保守できること」**

を重視します。

推測より確認を優先し、

短期的な便利さより長期的な保守性を優先し、

実装より先に必要な設計を確認します。
