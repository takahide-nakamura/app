---
interface Props {
  items: string[];
}

const { items } = Astro.props;
---

<ul class="proposal-list">
  {
    items.map((item) => (
      <li>{item}</li>
    ))
  }
</ul>

<style>
.proposal-list{
  line-height:1.9;
  padding-left:1.5rem;
}

.proposal-list li{
  margin-bottom:.6rem;
}
</style>