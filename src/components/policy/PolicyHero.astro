---
interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<section class="hero">
  <h1>{title}</h1>

  <p>{description}</p>
</section>

<style>
.hero{
  text-align:center;
  margin-bottom:var(--space-xl);
}
</style>