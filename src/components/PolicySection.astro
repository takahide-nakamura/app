---
interface Props {
  title: string;
  icon?: string;
}

const {
  title,
  icon
} = Astro.props;
---

<section class="policy-section">

  <h2 class="section-title">

    {
      icon && (
        <span
          class="section-icon"
          aria-hidden="true"
        >
          {icon}
        </span>
      )
    }

    <span>{title}</span>

  </h2>

  <div class="section-content">
    <slot />
  </div>

</section>

<style>
.policy-section {
  margin-top: 0;
  margin-bottom: 46px; /* セクション間の下マージン */
  padding: 0 !important;
}

.policy-section:last-child {
  margin-bottom: 0;
}

.section-title {
  display: flex;
  align-items: center;
  gap: 0.5rem;

  margin: 0 0 1rem 0;
  padding: 0 0 0.5rem 0;

  color: var(--color-primary, #003f88);

  font-size: 1.35rem;
  line-height: 1.4;
  border-bottom: 2px solid var(--color-border, #e5e7eb);
}

.section-icon {
  flex-shrink: 0;
}

.section-content {
  line-height: 1.8;
  padding: 0 !important;
}

/* 内部に挿入される全要素（関連する活動やコンテナ等）の余分な上下48px paddingを0に強制統一 */
:global(.section-content > *),
:global(.section-content div),
:global(.section-content article),
:global(.related-activities),
:global(.activity-card) {
  padding-top: 0 !important;
  padding-bottom: 0 !important;
}

/* 提案などの内部section（h3箇条書きブロック）のレイアウト調整 */
:global(.section-content section) {
  margin-bottom: 1.25rem !important;
  padding: 1.25rem 1.5rem !important;
  background-color: #f8fafc;
  border: 1px solid #e2e8f0;
  border-radius: 10px;
}

:global(.section-content section:last-child) {
  margin-bottom: 0 !important;
}

/* h3の見出し余白 */
:global(.section-content h3) {
  margin: 0 0 0.6rem 0 !important;
  padding: 0 !important;
  font-size: 1.1rem;
  font-weight: 700;
  color: var(--color-primary, #003f88);
}

/* 箇条書きリスト（ul / li）の余白と行間 */
:global(.section-content ul),
:global(.proposal-list) {
  margin: 0 !important;
  padding-left: 1.25rem !important;
}

:global(.section-content li) {
  margin-bottom: 0.5rem !important;
  line-height: 1.65 !important;
  font-size: 0.98rem;
}

:global(.section-content li:last-child) {
  margin-bottom: 0 !important;
}

@media (min-width: 768px) {
  .policy-section {
    margin-bottom: 46px;
  }

  .section-title {
    font-size: 1.45rem;
  }
}
</style>